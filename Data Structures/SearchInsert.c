#include <stdio.h>
#include <stdlib.h>

typedef struct Node
{	
	char data;
	struct node *lchild, *rchild;
}Node, No*;

No insert(No *r, char x){
	No p,q,s;
	s = (No)malloc(sizeof(Node));
	s->data = x;
	s->lchild = s->rchild = null;
	q=null;
	if(r=null) 
}